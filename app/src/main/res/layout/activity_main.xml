<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!-- Cabecera con fondo púrpura -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#673AB7"
        android:padding="16dp"
        android:text="TFG_PARTE1"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Título de resultados -->
    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="RESULTATS TESTS DE FIPS205"
        android:textAlignment="center"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Espacio -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="32dp" />

    <!-- Botón para ejecutar tests -->
    <Button
        android:id="@+id/runTestsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:background="#673AB7"
        android:padding="12dp"
        android:text="Ejecutar Tests de FIPS205"
        android:textColor="#FFFFFF" />

    <!-- Espacio -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Área de resultados -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <TextView
            android:id="@+id/testResultsTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Hello World!"
            android:textAlignment="center" />
    </ScrollView>

</LinearLayout>